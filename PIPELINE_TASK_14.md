# PIPELINE_TASK_14 — /app/tasks 页面功能重新设计（用户需求对齐 + Claude Code 执行）

## 背景与问题

用户反馈：`https://192.168.101.45/app/tasks` 功能「完全都错了」。
具体痛点：**S2 分镜拆出来的都是提示词**，不符合用户要求的结构化镜头组
（标题/内容/字幕/图片提示词/比例）。

现状核验（执枢 2026-08-11）：
- 项目：/Volumes/Data/GitHub/ai-video-studio，分支 feat/shop-video-frontend
- git HEAD = 380d8df（17:05）已有 S1-S6 rail + S5 复核面板 + S6 面板 + 三模式方案页
- **部署的 web 容器是 08:31 旧构建（9 步旧版）**——用户看到的 /app/tasks 是旧版，
  需在改造完成后重建 web 容器部署最新代码
- 用户参考对象：MoneyPrinterTurbo 体验（http://192.168.101.45:8501，音频设置部分做得好），
  但流程不符合用户要求，需按本任务书重做

## 用户完整需求（原文语义固化）

### 三种模式（方案页面配置）

**第一种 · 直接使用方案**
① 方案页面（配置）：
- 文案标题（留空则系统从文案内容提取）+ 文案内容
- 模型配置：分镜提示词模型（deepseek 等）、图片提示词模型、配音渠道
  （参考 MoneyPrinterTurbo 音频设置：音色/语速/音量/字幕）、视频模型

② 任务详情页 → 运行流水线（S1-S6）：
- S1 文案：用上面填的文案（可编辑、可重新生成）
- S2 分镜：调分镜提示词（下拉框可选，有默认）拆分文案 → 多组镜头：
  每组含 标题 / 内容 / 字幕 / 生成图片的提示词 / 图片比例
- S3 生图：用 S2 的提示词生图（生成后可编辑、可重新生成）
- S4 配音：用 S2 内容配音（可配置字幕）
- S5 复核：检查
- S6 生成视频：图片+配音+字幕合成，材料可编辑

**第二种 · AI 二创**
① 方案页面（配置）：
- 文案标题（留空则系统从文案内容提取）+ 二创原文案内容 + 二创提示词
- 模型配置：同上（分镜提示词模型、图片提示词模型、配音渠道、视频模型）

② 任务详情页 → 运行流水线（S1-S6）：
- S1 文案：通过二创提示词重新生成文案内容（可编辑、可重新生成）
- S2 分镜 / S3 生图 / S4 配音 / S5 复核 / S6 生成视频：同第一种

**第三种 · AI 创业**
① 方案页面（配置）：
- 商品库选品（一件商品）+ 二创提示词
- 模型配置：同上（分镜提示词模型、图片提示词模型、配音渠道、视频模型）

② 任务详情页 → 运行流水线（S1-S6）：
- S1 文案：通过二创提示词对选择的商品生成文案内容（可编辑、可重新生成）
- S2 分镜 / S3 生图 / S4 配音 / S5 复核 / S6 生成视频：同第一种

## 核心修正点（用户明确的「错」在哪）

1. **S2 分镜输出必须是结构化镜头组**：每镜头 = { 标题, 内容, 字幕, 图片提示词,
   图片比例 }，镜头编号如「镜头1 / 镜头2 / …」。**不是**一坨提示词。
   - 当前 s3.js BASE_PROMPT 输出字段为 index/title/scene/script/voiceover/
     subtitle/prompt/motion——需核对前端 S2 面板是否把 scene/script/prompt 混作
     提示词呈现；用户要的是「内容」为可读文案句、「图片提示词」单独成字段。
2. **S1 文案可编辑、可重新生成**：直接模式=直接用填的文案；二创/创业=经 LLM 生成。
3. **S3 生图可编辑、可重新生成**（单张/批量）。
4. **S4 配音可配置字幕**（开关/字号/位置），音色/语速/音量来自方案页配音渠道。
5. **S6 生成视频**：图片+配音+字幕合成，合成前材料可编辑。
6. 三模式方案页：模型配置四类（分镜提示词模型 / 图片提示词模型 / 配音渠道 / 视频模型）。

## 技术衔接（已有基础，勿重复造轮子）

- 方案配置页：`src/app/app/quick/page.tsx`（899 行，已有三 tab direct/rewrite/create
  骨架 + 四类模型配置提交，核对是否齐全）
- 任务详情：`src/components/app/wizard/WizardPage.tsx`（~2800 行，rail S1-S6 +
  每步 node editor 映射已存在，重点核对 S2 呈现与用户要求的差异）
- 分镜提示词预置：系统默认 prompts（通用解说/带货种草/故事叙事），下拉选择、有默认
- 商品库：`/app/products` 已有，选品后传商品信息到二创
- 配音渠道：复用现有 TTS 配置（/app/models 的 tts 渠道 + TTS 试听）
- 字幕配置：S4 字幕设置（参考现有 subtitle 逻辑：开关/字号/位置）
- 后端步骤：api/src/steps/s1.js … s9.js + routes/tasks.js（步骤状态机/重跑/续跑已有）
- rail 阶段定义：src/lib/app-data.ts（RAIL_STAGES_S6_*）

## 验收标准

1. 三种模式都能从方案页创建任务并进入 S1-S6
2. ① 直接模式：标题留空 → 系统从文案内容提取标题（任务标题非空）
3. S2 分镜下拉可选（默认选中「通用解说」），拆出的镜头**每镜含
   标题/内容/字幕/图片提示词/比例**，且镜头可增删改
4. S1/S3 可重新生成且结果可编辑（S3 重新生成后图片可单张替换）
5. S4 配音可配置字幕（开关/字号/位置）；S6 合成出片（i2v 视频生成为主，
   ffmpeg 静态合成为兜底）
6. S5 复核状态流转正确
7. 原有功能不回归（项目库/商品库/模板/素材/模型配置页正常）
8. **部署**：改造完成后重建 web 容器（docker compose up -d --build web），
   curl 验证 https://192.168.101.45/app/tasks 200

## 执行要求

- 分阶段 git 提交（每阶段可回滚）
- 每阶段输出：改动文件清单 + 校验记录
- 最终输出：部署验证结果
