# PIPELINE_TASK_13 — 快速生成三种模式 + 流水线重构 S1-S6

## 背景
- 用户参考 MoneyPrinterTurbo（http://192.168.101.45:8501）的体验：方案配置页 + 任务详情页流水线。
- 现有 /app/quick（PIPELINE_TASK_12）已有三 tab 雏形（direct/rewrite/create），现有 WizardPage 是 9/10 步流水线。
- 本次将：方案配置页扩展为三种模式；任务流水线重构为 S1-S6（每步可编辑/可重新生成）。

## 一、方案配置页（/app/quick 改造）

三种模式入口（tab 或卡片选择）：

| 模式 | 输入 | S1 文案来源 |
|---|---|---|
| ① 直接使用方案 | 标题（**留空则系统从文案内容提取**）+ 文案内容 | 直接用填的文案 |
| ② AI 二创 | 二创原文案 + 二创提示词 | 用二创提示词重新生成文案 |
| ③ AI 创业 | **商品库选品**（/app/products）+ 二创提示词 | 用二创提示词对商品生成文案 |

**统一模型配置**（四类，下拉选择，默认值来自现有模型配置中心 /app/models）：
1. 分镜提示词模型（LLM，如 deepseek）
2. 图片提示词模型（LLM/生图）
3. 配音渠道（参考 MoneyPrinterTurbo 音频设置：音色/语速/音量/字幕开关）
4. 视频模型（**i2v 视频生成**，走现有 i2v 渠道如 Kling；ffmpeg 静态合成仅作兜底/可选）

提交 → 创建任务 → 跳转任务详情页。

## 二、任务详情页流水线（WizardPage 重构为 S1-S6）

rail 从 9/10 步改为 6 步：

| 步骤 | 内容 | 操作 |
|---|---|---|
| S1 文案 | 方案页文案（按模式：直接用/二创生成/商品生成） | **可编辑、可重新生成**（重新生成调 LLM） |
| S2 分镜 | 调**分镜提示词**（**下拉框可选，预置 2-3 套系统默认**：通用解说/带货种草/故事叙事，有默认值）拆分文案 → 多组镜头 | 每个镜头含：**标题 / 内容 / 字幕 / 生成图片的提示词 / 图片比例**；镜头可增删改 |
| S3 生图 | 用 S2 的图片提示词生图 | **生成后可编辑、可重新生成**（单张/批量） |
| S4 配音 | 用 S2 内容配音 | 可配置**字幕**（开关/字号/位置），音色来自方案页配音渠道 |
| S5 复核 | 汇总检查（文案/镜头/图/音频/字幕） | 通过后进入 S6；不过则回到对应步骤修改 |
| S6 生成视频 | 图片+配音+字幕合成，**以 i2v 视频生成为主**（现有 i2v 渠道，如 Kling I2V） | **材料可编辑**（合成前可改任意材料，生成后可重新生成） |

## 三、技术衔接（已核对）

- 方案配置页：改 `src/app/app/quick/page.tsx`（现有三 tab 扩展为三模式 + 四类模型配置）
- 任务详情：改 `src/components/app/wizard/WizardPage.tsx`（2795 行，rail 步骤名/数量 + 每步 node editor 映射）
- 分镜提示词预置：新增系统默认 prompts（存于后端或前端常量），下拉选择
- 商品库对接：`/app/products` 已有，选品后传商品信息（图片/标题/卖点）到二创
- 配音渠道：复用现有 TTS 配置（/app/models 的 tts 渠道 + TTS 试听接口）
- 字幕配置：S4 加字幕设置（参考现有 subtitle 相关逻辑）

## 四、验收标准

1. 三种模式都能从方案页创建任务并进入 S1-S6
2. ① 标题留空 → 系统从文案提取标题（校验：生成任务标题非空）
3. S2 分镜提示词下拉可选（默认选中"通用解说"），拆出的镜头含标题/内容/字幕/图片提示词/比例
4. S1/S3 可重新生成且结果可编辑（S3 重新生成后图片可单张替换）
5. S4 配音可配置字幕；S6 **i2v 视频生成出片**（ffmpeg 静态合成为兜底）
6. S5 复核状态流转正确
7. 原有功能不回归（项目库/商品库/模板/素材/模型配置页正常）

## 五、执行要求

- 分阶段 git 提交（每阶段可回滚）
- 完成后跑通真实端到端（至少一条 mock 或真实链路）
- 不改：docker/nginx/数据库结构（除非必要且说明）
- 交付：改动文件列表 + 验证证据（页面截图/API 响应）
